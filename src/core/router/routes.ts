interface SwitchRoutes {
  root: string;
  home: string;
  aboutMe: string;
  project: string;
  contact: string;
}

export const switchRoutes: SwitchRoutes = {
  root: "/",
  home: "/home",
  aboutMe: "/aboutme",
  project: "/project",
  contact: "/contact",
}